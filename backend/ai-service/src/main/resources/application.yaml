server:
  port: 8081

spring:
  application:
    name: ai-service
  ai:
    google:
      genai:
        api-key: ${GEMINI_API_KEY}
        chat:
          options:
            model: gemini-1.5-flash